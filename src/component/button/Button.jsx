

const Button = (props) => {
      return (
            <>
                <button className="bg-[#060640] text-[#fff] py-[18px] px-[28px] rounded">{props.title}</button>  
            </>
      );
};

export default Button;